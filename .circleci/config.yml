version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:16.14.0
    parameters:
      build_environment:
        type: string
        default: stage
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-{{ checksum "package-lock.json" }}
      - run:
          name: install-packages
          command: npm install
      - run:
          name: build-app
          command: npm run build
      - save_cache:
          key: dependency-cache-{{ checksum "package-lock.json" }}
          paths:
            - ./node_modules
      - run:
          name: Configure Github credentials
          command: |
            git config user.email $GITHUB_EMAIL
            git config user.name $GITHUB_USERNAME
      - add_ssh_keys:
          fingerprints:
            - $GITHUB_FINGERPRINT
      - run:
          name: Deploy to Azure Web App
          command: npm run deploy-<< parameters.build_environment >>

workflows:
  deploy:
    jobs:
      - build:
          build_environment: prod
          filters:
            branches:
              only: master
      - build:
          build_environment: stage
          filters:
            branches:
              only: development
